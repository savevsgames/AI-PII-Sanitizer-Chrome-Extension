{
  "name": "ai-pii-sanitizer",
  "version": "0.1.0",
  "description": "Chrome extension that sanitizes PII in AI chat services using bidirectional aliasing",
  "private": true,
  "scripts": {
    "build": "webpack --mode production",
    "build:release": "npm run build && node scripts/package-release.js",
    "dev": "webpack --mode development --watch",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "jest --testPathIgnorePatterns=e2e --testPathIgnorePatterns=integration",
    "test:integration": "jest --testMatch='**/tests/integration/**/*.test.ts' --runInBand",
    "test:e2e": "jest --testMatch='**/tests/e2e-puppeteer/**/*.test.ts' --config=tests/e2e-puppeteer/jest.config.js",
    "test:e2e:core": "jest --testMatch='**/tests/e2e-puppeteer/core/**/*.test.ts' --config=tests/e2e-puppeteer/jest.config.js",
    "test:e2e:features": "jest --testMatch='**/tests/e2e-puppeteer/features/**/*.test.ts' --config=tests/e2e-puppeteer/jest.config.js",
    "test:e2e:full": "npm run build && npm run test:e2e",
    "test:all": "npm run test:unit && npm run test:integration && npm run test:coverage && npm run build",
    "test:all:puppeteer": "npm run test:unit && npm run test:integration && npm run test:e2e && npm run test:coverage && npm run build",
    "test:all:selenium": "npm run test:unit && npm run test:integration && npm run test:e2e:selenium && npm run test:coverage && npm run build",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --testPathIgnorePatterns=integration --testPathIgnorePatterns=e2e",
    "test:e2e:selenium": "cd tests/e2e-selenium && pytest",
    "test:e2e:selenium:smoke": "cd tests/e2e-selenium && pytest -m smoke",
    "test:e2e:selenium:critical": "cd tests/e2e-selenium && pytest -m critical",
    "test:e2e:selenium:report": "cd tests/e2e-selenium && pytest --alluredir=./reports/allure-results && allure serve ./reports/allure-results",
    "test:e2e:selenium:verbose": "cd tests/e2e-selenium && pytest -v -s",
    "lint": "eslint src/**/*.ts",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "chrome-extension",
    "privacy",
    "pii",
    "ai",
    "chatgpt",
    "claude"
  ],
  "author": "",
  "license": "AGPL-3.0",
  "devDependencies": {
    "@peculiar/webcrypto": "^1.5.0",
    "@playwright/test": "^1.56.0",
    "@types/chrome": "^0.0.268",
    "@types/dompurify": "^3.0.5",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.7",
    "@types/puppeteer": "^5.4.7",
    "@typescript-eslint/eslint-plugin": "^7.7.0",
    "@typescript-eslint/parser": "^7.7.0",
    "copy-webpack-plugin": "^12.0.2",
    "cross-fetch": "^4.1.0",
    "dotenv-webpack": "^8.1.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.2.0",
    "node-fetch": "^2.7.0",
    "puppeteer": "^24.29.1",
    "puppeteer-core": "^24.29.1",
    "ts-jest": "^29.1.2",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "dompurify": "^3.3.0",
    "firebase": "^12.5.0",
    "mammoth": "^1.11.0",
    "pdfjs-dist": "^5.4.394",
    "zustand": "^5.0.8"
  }
}
