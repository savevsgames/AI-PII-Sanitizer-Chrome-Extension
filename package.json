{
  "name": "ai-pii-sanitizer",
  "version": "0.1.0",
  "description": "Chrome extension that sanitizes PII in AI chat services using bidirectional aliasing",
  "private": true,
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "jest --testPathIgnorePatterns=e2e --testPathIgnorePatterns=integration",
    "test:integration": "jest --testMatch='**/tests/integration/**/*.test.ts' --runInBand",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:full": "npm run build && npm run test:e2e",
    "test:all": "npm run test:unit && npm run test:integration && npm run test:e2e && npm run test:coverage && npm run build",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --testPathIgnorePatterns=integration --testPathIgnorePatterns=e2e",
    "lint": "eslint src/**/*.ts",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "chrome-extension",
    "privacy",
    "pii",
    "ai",
    "chatgpt",
    "claude"
  ],
  "author": "",
  "license": "AGPL-3.0",
  "devDependencies": {
    "@peculiar/webcrypto": "^1.5.0",
    "@playwright/test": "^1.56.0",
    "@types/chrome": "^0.0.268",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.7",
    "@typescript-eslint/eslint-plugin": "^7.7.0",
    "@typescript-eslint/parser": "^7.7.0",
    "copy-webpack-plugin": "^12.0.2",
    "cross-fetch": "^4.1.0",
    "dotenv-webpack": "^8.1.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.2.0",
    "node-fetch": "^2.7.0",
    "ts-jest": "^29.1.2",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "firebase": "^12.5.0",
    "mammoth": "^1.11.0",
    "pdfjs-dist": "^5.4.394",
    "zustand": "^5.0.8"
  }
}
